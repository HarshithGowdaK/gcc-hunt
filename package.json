{
  "name": "gcc-hunt-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "scrape": "node scripts/scrape_local.js"
  },
  "dependencies": {
    "axios": "^1.17.0",
    "playwright": "^1.60.0",
    "xlsx": "^0.18.5"
  }
}
